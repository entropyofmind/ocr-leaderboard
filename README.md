# Discord OCR Leaderboard Bot
Automatically reads screenshots in one channel and keeps a live Top 20 leaderboard in another.

Deploy with Railway / Fly.io / Render in one click.
Token is stored safely as a secret (DISCORD_TOKEN).
